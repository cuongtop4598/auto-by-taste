---
phase: 02-mac-hardware-data
plan: 03
type: execute
wave: 1
depends_on:
  - "02-02"
files_modified:
  - App.tsx
autonomous: true
requirements:
  - R4
gap_closure: true

must_haves:
  truths:
    - "User can see ChipComparison component in the running application"
    - "ChipComparison section has navigation anchor (id attribute)"
    - "Component renders in appropriate position within page flow"
  artifacts:
    - path: "App.tsx"
      provides: "ChipComparison integration"
      contains: "ChipComparison"
  key_links:
    - from: "App.tsx"
      to: "components/ChipComparison.tsx"
      via: "import and render"
      pattern: "import.*ChipComparison.*from.*components/ChipComparison"
---

<objective>
Integrate ChipComparison component into the main application.

Purpose: Close Gap 1 from verification - ChipComparison.tsx exists (299 LOC, feature-complete) but is NOT imported or rendered in App.tsx. Users cannot see the M-series chip comparison, which is a core deliverable of Phase 2 and R4 requirement.

Output: ChipComparison component visible in running application with navigation anchor.
</objective>

<execution_context>
@/home/anonymouse/.claude/get-shit-done/workflows/execute-plan.md
@/home/anonymouse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-mac-hardware-data/02-VERIFICATION.md

<interfaces>
<!-- From existing App.tsx structure -->

App.tsx imports and renders components in sections with IDs:
```typescript
import { ChipComparison } from './components/ChipComparison';

// Section pattern:
<section id="chip-comparison">
  <ChipComparison />
</section>
```

Current App.tsx section order (for placement context):
1. #overview - Hero
2. #problem - ProblemSolution
3. #segments - TargetSegments
4. #architecture - AIArchitectureGraph
5. #org-chart - OrgChartAgents
6. #models - ModelHardwareGraph
7. #products - ProductTiers
8. #business - BusinessModel
9. #strategy - StrategySection
10. #pricing - ServicePricing

ChipComparison should be placed near #models (ModelHardwareGraph) since both relate to hardware/chip information.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate ChipComparison into App.tsx</name>
  <files>App.tsx</files>
  <action>
    1. Add import statement at top of App.tsx:
       ```typescript
       import { ChipComparison } from './components/ChipComparison';
       ```

    2. Add ChipComparison section BEFORE the #models section (ModelHardwareGraph).
       Rationale: ChipComparison provides chip context that enhances understanding
       of ModelHardwareGraph's AI model data. Place it between #org-chart and #models:

       ```tsx
       <section id="chip-comparison">
         <ChipComparison />
       </section>

       <section id="models">
         <ModelHardwareGraph />
       </section>
       ```

    3. Verify the import uses named export syntax (ChipComparison uses named export,
       not default export).

    Placement logic:
    - After #org-chart (OrgChartAgents) - shifts from organization to hardware
    - Before #models (ModelHardwareGraph) - chip specs provide context for AI model requirements
    - This creates logical flow: agents -> chip capabilities -> AI model requirements
  </action>
  <verify>
    <automated>npm run build 2>&1 | head -50</automated>
  </verify>
  <done>
    - ChipComparison imported in App.tsx
    - ChipComparison rendered in section with id="chip-comparison"
    - Section placed appropriately in page flow (before #models)
    - Build succeeds with no TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify component renders in browser</name>
  <files>App.tsx</files>
  <action>
    1. Start development server if not running
    2. Open browser to localhost URL
    3. Verify ChipComparison section is visible on page
    4. Verify chart renders with M-series chip data
    5. Verify section can be navigated to via #chip-comparison anchor

    Expected behavior:
    - Chart displays M1 Pro, M2 Pro, M3 Pro, M4 Pro in generation view
    - Vietnamese title "So sanh chip M-series" visible
    - View toggle buttons work ("Theo the he" / "Tat ca chip")
    - Details panel updates when clicking chart bars
    - Glass-card styling matches other sections
  </action>
  <verify>
    <automated>curl -s http://localhost:5173 2>/dev/null | grep -c "chip-comparison" || echo "Dev server not running - verify manually"</automated>
  </verify>
  <done>
    - ChipComparison visible in running application
    - Chart renders correctly with chip data
    - Navigation anchor #chip-comparison works
    - Component matches page styling
  </done>
</task>

</tasks>

<verification>
Build verification:
```bash
npm run build
```

Development server verification:
```bash
npm run dev
# Open http://localhost:5173/#chip-comparison
```

Visual checks:
1. ChipComparison section visible between OrgChartAgents and ModelHardwareGraph
2. Chart renders with M-series chip comparison data
3. Interactions work (view toggle, chip selection)
4. Section accessible via #chip-comparison anchor
</verification>

<success_criteria>
- [ ] ChipComparison imported in App.tsx
- [ ] ChipComparison rendered in section with id="chip-comparison"
- [ ] Section positioned appropriately in page flow
- [ ] Build succeeds with no errors
- [ ] Component visible in browser when running dev server
- [ ] R4 requirement now fully satisfied (visual comparison accessible to users)
</success_criteria>

<output>
After completion, create `.planning/phases/02-mac-hardware-data/02-03-SUMMARY.md`
</output>
